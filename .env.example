# .env
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_postgres_user
DB_PASSWORD=your_postgres_password
DB_NAME=seryu_cargo_db
PORT=3000

# Provided in variable_configs, but good to have a default if DB lookup fails or for testing
# DEFAULT_DRIVER_MONTHLY_ATTENDANCE_SALARY=50000